# 百万广告系统数据库初始化执行说明

## 推荐用法：空数据库首次部署

新部署环境优先执行：

```sql
SOURCE 数据库初始化_正式系统V1_一键版.sql;
```

如果使用 Navicat、DBeaver、宝塔数据库面板等图形工具，直接打开 `数据库初始化_正式系统V1_一键版.sql`，选择目标库后执行整份脚本即可。

这份脚本已经包含：

- 正式系统 V1 当前代码需要的核心表结构
- RBAC 角色、菜单、按钮权限、角色授权
- 登录日志表
- 13 类演示账号
- 广告主、代理商、广告、素材、计划、设备、下发、播放日志、报表、账单、工单、反馈演示数据

## 默认演示账号

所有账号初始密码均为：

```text
admin123
```

首次登录成功后，系统会自动把明文密码升级为 BCrypt 加密密码。

| 账号 | 角色 |
| --- | --- |
| `admin@mysher.com` | 超级管理员 |
| `developer@mysher.com` | 开发工程师 |
| `operator@mysher.com` | 运维工程师 |
| `region@mysher.com` | 区域管理员 |
| `business@mysher.com` | 业务运营 |
| `reviewer@mysher.com` | 广告素材审核 |
| `finance@mysher.com` | 财务账号 |
| `analyst@mysher.com` | 数据分析师 |
| `agent01@mysher.com` | 广告代理商账号 |
| `advertiser01@mysher.com` | 广告主账号 |
| `service@mysher.com` | 客服账号 |
| `auditor@mysher.com` | 审计账号 |
| `audience@mysher.com` | 受众账号 |

## 已有数据库的处理建议

如果当前库已经执行过早期脚本，尤其是 `正式系统V1数据库建表SQL初稿.sql`，不要直接混跑所有脚本。

原因是早期初稿里的 `sys_user`、`sys_role`、`sys_menu`、`sys_login_log` 字段和当前正式代码使用的 RBAC 字段不完全一致。MySQL 5.x 又不支持 `ADD COLUMN IF NOT EXISTS`，盲目重复执行补丁容易出现“字段已存在”或“字段不匹配”。

建议流程：

1. 先备份当前数据库。
2. 如果只是本地开发或演示库，建议新建空库后执行 `数据库初始化_正式系统V1_一键版.sql`。
3. 如果是保留历史数据的库，先对照当前脚本中的表结构做迁移，重点检查 `sys_user`、`sys_role`、`sys_menu`、`sys_login_log`。
4. 历史补丁脚本仅作为排查参考，不再作为新部署首选入口。

## 验证清单

执行完成后建议按顺序验证：

1. 使用 `admin@mysher.com / admin123` 登录。
2. 进入“用户管理”，确认能看到 13 个账号。
3. 进入“权限管理”，确认角色菜单树能加载并保存。
4. 进入“广告列表”，确认能看到演示广告。
5. 进入“素材管理”，确认已审核素材可被计划选择。
6. 进入“投放计划”，确认演示计划、设备和素材关联正常。
7. 进入“数据报表”和“账单结算”，确认演示统计数据存在。
8. 使用 `advertiser01@mysher.com / admin123` 登录，确认只能看到广告主相关数据。
9. 使用 `agent01@mysher.com / admin123` 登录，确认只能看到代理商相关数据。
10. 故意输错密码后，再到“用户管理”的登录日志区域查看失败记录。

## 与旧脚本的关系

保留旧脚本是为了追溯开发过程和已有库补丁：

- `正式系统V1数据库建表SQL初稿.sql`：早期建表初稿，不建议新部署优先使用。
- `数据库补丁_20260702_正式RBAC基础表.sql`：早期 RBAC 基础补丁，已合并进一键版。
- `数据库补丁_20260703_角色权限页面.sql`：权限管理菜单补丁，已合并进一键版。
- `数据库补丁_20260703_登录安全加固.sql`：登录日志表补丁，已合并进一键版。
- `数据库补丁_20260702_*审计字段.sql`：早期表结构修复补丁，一键版已经按当前结构建表。
