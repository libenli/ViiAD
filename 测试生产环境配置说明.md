# 百万广告系统测试/生产环境配置说明

## 后端 Profile

后端支持三套 profile：

- `dev`：本地开发，默认数据库 `ad_admin`，上传文件保存到本地 `./uploads`。
- `test`：测试服务器，默认使用 OSS 上传，数据库和 OSS 配置从环境变量读取。
- `prod`：生产服务器，默认使用 OSS 上传，数据库和 OSS 配置必须从环境变量读取。

启动测试环境示例：

```bash
java -jar ad-system.jar --spring.profiles.active=test
```

启动生产环境示例：

```bash
java -jar ad-system.jar --spring.profiles.active=prod
```

## 后端环境变量

测试/生产服务器建议配置：

```bash
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=test

MRD_DB_URL=jdbc:mysql://127.0.0.1:3306/ad_admin_test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
MRD_DB_USERNAME=ad_admin
MRD_DB_PASSWORD=你的数据库密码

MRD_REDIS_HOST=127.0.0.1
MRD_REDIS_PORT=6379
MRD_REDIS_DATABASE=1
MRD_REDIS_PASSWORD=你的Redis密码

MRD_AUTH_CODE_MOCK_SEND=false

MRD_SMS_ACCESS_KEY_ID=你的短信AccessKeyId
MRD_SMS_ACCESS_KEY_SECRET=你的短信AccessKeySecret
MRD_SMS_SIGN_NAME=你的短信签名
MRD_SMS_REGION_ID=cn-hangzhou
MRD_SMS_TEMPLATE_ZH=你的中文验证码模板Code
MRD_SMS_TEMPLATE_EN=你的英文验证码模板Code
MRD_SMS_TEMPLATE_OVERSEAS=你的海外验证码模板Code

MRD_EMAIL_HOST=smtp.example.com
MRD_EMAIL_PORT=465
MRD_EMAIL_USERNAME=你的邮箱账号
MRD_EMAIL_PASSWORD=你的邮箱授权码或密码
MRD_EMAIL_FROM=你的发件邮箱
MRD_EMAIL_FROM_NAME=ViiAD
MRD_EMAIL_SSL=true
MRD_EMAIL_STARTTLS=false

MRD_UPLOAD_PROVIDER=oss
MRD_UPLOAD_BASE_PATH=materials/test
MRD_UPLOAD_MAX_FILE_SIZE=500MB
MRD_UPLOAD_MAX_REQUEST_SIZE=520MB

MRD_OSS_ENDPOINT=oss-cn-shanghai.aliyuncs.com
MRD_OSS_ACCESS_KEY_ID=你的AccessKeyId
MRD_OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
MRD_OSS_BUCKET_NAME=你的Bucket名称
MRD_OSS_PUBLIC_BASE_URL=https://你的Bucket访问域名或自定义域名
```

生产环境把 `SPRING_PROFILES_ACTIVE=prod`，并建议把 `MRD_UPLOAD_BASE_PATH` 改成 `materials/prod`。

## 前端构建

测试环境构建：

```bash
npm run build:test
```

生产环境构建：

```bash
npm run build:prod
```

当前前端 `VITE_API_BASE_URL=/api`，适合 Nginx 同域反代：

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:8080/api/;
}
```

## 素材上传链路

素材表单现在支持直接上传图片、视频、HTML 或 ZIP 文件：

1. 前端调用 `POST /api/files/upload`。
2. 后端根据 `mrd.upload.provider` 决定上传到本地或 OSS。
3. `test/prod` 默认上传到 OSS。
4. 上传成功后返回文件 URL，前端自动回填到素材表单 `fileUrl`。
5. 保存素材时，业务表仍保存最终 URL，后续大屏播放直接使用该地址。

## OSS 注意事项

- OSS Bucket 或自定义域名需要能被浏览器和大屏设备访问。
- 如果前端需要预览视频或图片，OSS 访问域名建议配置 CORS。
- 上传大视频时，Nginx 也要同步放开请求体大小，例如 `client_max_body_size 500m;`。
- AccessKey 不要写进前端，也不要提交到代码仓库，只放服务器环境变量。

## 验证码与忘记密码链路

登录页支持手机验证码、邮箱验证码登录；忘记密码采用“发送验证码、校验验证码、重置密码”三步流程。

1. 前端发送 `POST /api/auth/verification-code/send` 获取登录验证码。
2. 前端发送 `POST /api/auth/login/code` 完成手机或邮箱验证码登录。
3. 忘记密码时，前端发送 `POST /api/auth/forgot-password/code` 获取验证码。
4. 前端发送 `POST /api/auth/forgot-password/validate` 校验验证码并获取一次性 `resetToken`。
5. 前端发送 `POST /api/auth/forgot-password/reset` 提交新密码。

测试/生产环境建议设置 `MRD_AUTH_CODE_MOCK_SEND=false`，并配置短信与邮箱参数。开发环境默认仍可使用 mock 模式，便于本机无短信或 SMTP 配置时调试流程。
