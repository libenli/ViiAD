# 数据库字段一致性检查报告 20260702

## 1. 本次报错原因

新建广告时报错：

```text
Unknown column 'create_by' in 'field list'
```

原因是后台 `AdOrder` 继承了 `BaseEntity`，而 `BaseEntity` 包含：

- `id`
- `create_by`
- `create_time`
- `update_by`
- `update_time`
- `deleted`

因此只要某个实体继承 `BaseEntity`，对应数据库表就应尽量包含这些基础字段。

## 2. 当前代码层面检查结果

当前正式后台工程里，已经存在并继承 `BaseEntity` 的业务实体只有：

| 实体 | 表名 | 当前风险 |
| --- | --- | --- |
| `AdOrder` | `ad_order` | 已发现并修复 |

所以从“当前已写代码”来看，马上会触发同类错误的是 `ad_order`。

## 3. 数据库建表 SQL 层面潜在风险

如果后续素材、计划、设备、账单、工单等实体也按统一规范继承 `BaseEntity`，以下表会有同类风险。

### 3.1 已缺少部分审计字段的核心业务表

| 表名 | 已有字段 | 缺少字段 | 建议 |
| --- | --- | --- | --- |
| `ad_advertiser` | `create_by`, `create_time`, `update_time`, `deleted` | `update_by` | 补充 |
| `ad_agent` | `create_by`, `create_time`, `update_time`, `deleted` | `update_by` | 补充 |
| `ad_material` | `create_time`, `update_time`, `deleted` | `create_by`, `update_by` | 补充 |
| `ad_plan` | `create_user_id`, `create_time`, `update_time`, `deleted` | `create_by`, `update_by` | 补充 |
| `ad_building` | `create_time`, `update_time`, `deleted` | `create_by`, `update_by` | 补充 |
| `ad_device` | `create_time`, `update_time`, `deleted` | `create_by`, `update_by` | 补充 |
| `ad_work_order` | `create_time`, `update_time` | `create_by`, `update_by`, `deleted` | 补充 |
| `ad_feedback` | `create_time`, `update_time` | `create_by`, `update_by`, `deleted` | 补充 |

### 3.2 系统配置表

| 表名 | 风险说明 | 建议 |
| --- | --- | --- |
| `sys_menu` | 如果菜单实体继承 `BaseEntity`，会缺少 `create_by`, `update_by`, `deleted` | 建议补充 |
| `sys_dict` | 如果字典实体继承 `BaseEntity`，会缺少 `create_by`, `update_by`, `update_time`, `deleted` | 建议补充 |
| `sys_dict_item` | 如果字典项实体继承 `BaseEntity`，会缺少 `create_by`, `create_time`, `update_by`, `update_time`, `deleted` | 建议补充 |

### 3.3 不建议强行继承 BaseEntity 的表

以下表更像关联表、日志表或统计结果表，不一定需要继承 `BaseEntity`：

| 表名 | 建议 |
| --- | --- |
| `sys_user_role` | 关联表，保持轻量即可 |
| `sys_role_menu` | 关联表，保持轻量即可 |
| `ad_plan_material` | 关联表，保持轻量即可 |
| `ad_plan_device` | 关联表，保持轻量即可 |
| `ad_plan_time_slot` | 计划子表，可不继承 |
| `ad_plan_audience_tag` | 计划子表，可不继承 |
| `ad_device_log` | 日志表，可单独建模 |
| `ad_delivery_record` | 执行记录表，可单独建模 |
| `ad_play_log` | 播放日志表，可单独建模 |
| `ad_report_daily` | 报表汇总表，可单独建模 |
| `ad_bill_detail` | 账单明细表，可单独建模 |
| `sys_oper_log` | 操作日志表，可单独建模 |
| `sys_login_log` | 登录日志表，可单独建模 |

## 4. 结论

当前广告保存问题已经定位为 `ad_order` 缺少审计字段。

为了后续开发更顺，建议核心主数据表统一补齐：

- `create_by`
- `create_time`
- `update_by`
- `update_time`
- `deleted`

已另外提供补丁：

```text
数据库补丁_20260702_核心表补充审计字段.sql
```

