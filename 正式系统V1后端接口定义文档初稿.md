# 百万广告系统正式版 V1 后端接口定义文档初稿

## 1. 通用约定

### 1.1 接口前缀

```text
/api
```

### 1.2 认证方式

登录成功后返回 `token`，前端请求时统一放入请求头：

```http
Authorization: Bearer <token>
```

### 1.3 统一返回结构

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 1.4 分页返回结构

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 0,
    "page": 1,
    "size": 10
  }
}
```

### 1.5 通用分页参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | Integer | 否 | 页码，默认 1 |
| size | Integer | 否 | 每页数量，默认 10 |
| keyword | String | 否 | 关键词 |
| status | String | 否 | 状态 |
| startTime | String | 否 | 开始时间 |
| endTime | String | 否 | 结束时间 |

### 1.6 常用状态码

| code | 说明 |
| --- | --- |
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录或 token 失效 |
| 403 | 无权限 |
| 404 | 数据不存在 |
| 500 | 服务异常 |

## 2. 认证接口

### 2.1 登录

```http
POST /api/auth/login
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| username | String | 是 | 登录账号 |
| password | String | 是 | 登录密码 |
| captcha | String | 否 | 验证码 |
| uuid | String | 否 | 验证码标识 |

返回数据：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| token | String | 访问令牌 |
| userId | Long | 用户ID |
| username | String | 登录账号 |
| realName | String | 真实姓名 |

### 2.2 退出登录

```http
POST /api/auth/logout
```

### 2.3 当前用户信息

```http
GET /api/auth/info
```

返回数据：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user | Object | 用户信息 |
| roles | Array | 角色编码 |
| permissions | Array | 权限码 |
| dataScope | String | 数据范围 |

### 2.4 当前用户菜单

```http
GET /api/auth/menus
```

## 3. 系统管理接口

### 3.1 用户分页

```http
GET /api/system/users
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 用户名、姓名、手机号 |
| deptId | Long | 部门ID |
| status | Integer | 状态 |
| page | Integer | 页码 |
| size | Integer | 每页数量 |

### 3.2 新建用户

```http
POST /api/system/users
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| username | String | 是 | 登录账号 |
| password | String | 是 | 密码 |
| realName | String | 是 | 真实姓名 |
| phone | String | 否 | 手机号 |
| email | String | 否 | 邮箱 |
| deptId | Long | 否 | 部门ID |
| roleIds | Array | 是 | 角色ID |

### 3.3 更新用户

```http
PUT /api/system/users/{id}
```

### 3.4 删除用户

```http
DELETE /api/system/users/{id}
```

### 3.5 角色分页

```http
GET /api/system/roles
```

### 3.6 新建角色

```http
POST /api/system/roles
```

### 3.7 更新角色

```http
PUT /api/system/roles/{id}
```

### 3.8 分配角色菜单

```http
PUT /api/system/roles/{id}/menus
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| menuIds | Array | 是 | 菜单ID列表 |

### 3.9 菜单树

```http
GET /api/system/menus/tree
```

### 3.10 部门树

```http
GET /api/system/depts/tree
```

## 4. 广告主与代理商接口

### 4.1 广告主分页

```http
GET /api/advertisers
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 广告主编号、名称、联系人 |
| status | String | 状态 |
| page | Integer | 页码 |
| size | Integer | 每页数量 |

### 4.2 新建广告主

```http
POST /api/advertisers
```

### 4.3 更新广告主

```http
PUT /api/advertisers/{id}
```

### 4.4 广告主详情

```http
GET /api/advertisers/{id}
```

### 4.5 代理商分页

```http
GET /api/agents
```

### 4.6 新建代理商

```http
POST /api/agents
```

### 4.7 更新代理商

```http
PUT /api/agents/{id}
```

### 4.8 代理商详情

```http
GET /api/agents/{id}
```

## 5. 广告接口

### 5.1 广告分页

```http
GET /api/ads
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 广告编号、广告名称 |
| advertiserId | Long | 广告主ID |
| agentId | Long | 代理商ID |
| regionCode | String | 区域编码 |
| adType | String | 广告类型 |
| status | String | 状态 |
| startTime | String | 创建开始时间 |
| endTime | String | 创建结束时间 |
| page | Integer | 页码 |
| size | Integer | 每页数量 |

### 5.2 新建广告

```http
POST /api/ads
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| adName | String | 是 | 广告名称 |
| advertiserId | Long | 是 | 广告主ID |
| agentId | Long | 否 | 代理商ID |
| adType | String | 是 | 广告类型 |
| objective | String | 否 | 投放目标 |
| regionCode | String | 否 | 区域编码 |
| budgetAmount | Decimal | 否 | 预算 |
| description | String | 否 | 说明 |

### 5.3 广告详情

```http
GET /api/ads/{id}
```

返回建议包含：

- 广告基础信息
- 关联素材列表
- 关联计划列表
- 操作日志

### 5.4 更新广告

```http
PUT /api/ads/{id}
```

### 5.5 提交广告

```http
POST /api/ads/{id}/submit
```

### 5.6 审核通过广告

```http
POST /api/ads/{id}/approve
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| comment | String | 否 | 审核意见 |

### 5.7 驳回广告

```http
POST /api/ads/{id}/reject
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| comment | String | 是 | 驳回原因 |

## 6. 素材接口

### 6.1 素材分页

```http
GET /api/materials
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 素材编号、素材名称 |
| adId | Long | 广告ID |
| materialType | String | 素材类型 |
| status | String | 审核状态 |
| uploaderId | Long | 上传人 |
| startTime | String | 上传开始时间 |
| endTime | String | 上传结束时间 |

### 6.2 上传文件

```http
POST /api/materials/upload
```

请求类型：

```http
multipart/form-data
```

返回数据：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| fileUrl | String | 文件地址 |
| fileSize | Long | 文件大小 |
| coverUrl | String | 封面地址 |
| durationSeconds | Integer | 视频时长 |
| width | Integer | 宽度 |
| height | Integer | 高度 |

### 6.3 新建素材

```http
POST /api/materials
```

### 6.4 素材详情

```http
GET /api/materials/{id}
```

### 6.5 更新素材

```http
PUT /api/materials/{id}
```

### 6.6 提交素材审核

```http
POST /api/materials/{id}/submit
```

### 6.7 审核通过素材

```http
POST /api/materials/{id}/approve
```

### 6.8 驳回素材

```http
POST /api/materials/{id}/reject
```

## 7. 投放计划接口

### 7.1 计划分页

```http
GET /api/plans
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 计划编号、计划名称 |
| adId | Long | 广告ID |
| regionCode | String | 区域编码 |
| scheduleStatus | String | 排期状态 |
| deliveryStatus | String | 投放状态 |
| startTime | String | 投放开始时间 |
| endTime | String | 投放结束时间 |

### 7.2 新建计划

```http
POST /api/plans
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| planName | String | 是 | 计划名称 |
| adId | Long | 是 | 广告ID |
| materialIds | Array | 是 | 素材ID列表 |
| deviceIds | Array | 是 | 设备ID列表 |
| regionCode | String | 否 | 区域编码 |
| startTime | String | 是 | 开始时间 |
| endTime | String | 是 | 结束时间 |
| timeSlots | Array | 否 | 投放时段 |
| audienceTags | Array | 否 | 人群标签 |

### 7.3 计划详情

```http
GET /api/plans/{id}
```

### 7.4 更新计划

```http
PUT /api/plans/{id}
```

### 7.5 提交排期

```http
POST /api/plans/{id}/schedule
```

### 7.6 启动计划

```http
POST /api/plans/{id}/start
```

### 7.7 暂停计划

```http
POST /api/plans/{id}/pause
```

### 7.8 结束计划

```http
POST /api/plans/{id}/finish
```

## 8. 设备与下发接口

### 8.1 楼宇分页

```http
GET /api/buildings
```

### 8.2 新建楼宇

```http
POST /api/buildings
```

### 8.3 设备分页

```http
GET /api/devices
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 设备编号、设备名称 |
| buildingId | Long | 楼宇ID |
| regionCode | String | 区域编码 |
| onlineStatus | String | 在线状态 |
| faultStatus | String | 故障状态 |

### 8.4 设备详情

```http
GET /api/devices/{id}
```

### 8.5 更新设备

```http
PUT /api/devices/{id}
```

### 8.6 设备日志

```http
GET /api/devices/{id}/logs
```

### 8.7 下发记录分页

```http
GET /api/deliveries
```

### 8.8 重试下发

```http
POST /api/deliveries/{id}/retry
```

### 8.9 播放日志

```http
GET /api/play-logs
```

## 9. 报表接口

### 9.1 报表概览

```http
GET /api/reports/overview
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| startDate | String | 开始日期 |
| endDate | String | 结束日期 |
| regionCode | String | 区域编码 |

### 9.2 广告报表

```http
GET /api/reports/ads
```

### 9.3 区域报表

```http
GET /api/reports/regions
```

### 9.4 设备报表

```http
GET /api/reports/devices
```

### 9.5 广告主报表

```http
GET /api/reports/advertisers
```

### 9.6 代理商报表

```http
GET /api/reports/agents
```

## 10. 财务接口

### 10.1 账单分页

```http
GET /api/bills
```

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| keyword | String | 账单编号 |
| billType | String | 账单类型 |
| advertiserId | Long | 广告主ID |
| agentId | Long | 代理商ID |
| billMonth | String | 账单月份 |
| status | String | 状态 |

### 10.2 账单详情

```http
GET /api/bills/{id}
```

### 10.3 确认账单

```http
POST /api/bills/{id}/confirm
```

### 10.4 标记支付

```http
POST /api/bills/{id}/pay
```

## 11. 工单与反馈接口

### 11.1 工单分页

```http
GET /api/workorders
```

### 11.2 新建工单

```http
POST /api/workorders
```

### 11.3 更新工单

```http
PUT /api/workorders/{id}
```

### 11.4 分派工单

```http
POST /api/workorders/{id}/assign
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| assigneeId | Long | 是 | 处理人ID |

### 11.5 关闭工单

```http
POST /api/workorders/{id}/close
```

### 11.6 反馈分页

```http
GET /api/feedbacks
```

### 11.7 新建反馈

```http
POST /api/feedbacks
```

### 11.8 处理反馈

```http
POST /api/feedbacks/{id}/process
```

## 12. 日志接口

### 12.1 操作日志分页

```http
GET /api/logs/operations
```

### 12.2 登录日志分页

```http
GET /api/logs/logins
```

## 13. 前后端联调优先级

1. 登录、当前用户、菜单、权限码
2. 广告列表、广告创建、广告详情
3. 素材上传、素材提交、素材审核
4. 计划创建、计划启动、计划暂停
5. 设备列表、下发记录、播放日志
6. 报表概览、广告报表
7. 账单列表、账单详情
8. 工单、反馈、日志

